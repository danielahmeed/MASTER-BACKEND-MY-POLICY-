server.port=8083
spring.application.name=metadata-service

# Centralized Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/mypolicy_db
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# For JSONB support if needed
spring.jpa.properties.hibernate.connection.release_mode=after_transaction

# Optional: Table prefix for metadata schema separation
# spring.jpa.properties.hibernate.default_schema=metadata
